"""
Entidades - Tabelas

Tem atributos - colunas
Primary Key - PK
    Identificador de cada registro na tabela,valor de chave é único, não se
    repete, não pode ser nulo, tem que haver apenas uma primary key po tabela.
Foreign Key - FK
    A coluna que possui uma FK referência outra coluna em outra tabela, e
    geralmente a FK passa a ser a PK na outra tabela
"""
